# Subtask 0001.10: JSON Serialization Tests

**Parent Task**: 0001 - Public API Finalization  
**Effort**: M (2-3h)  
**Dependencies**: 0001.7, 0001.8, 0001.9

## Description

Create comprehensive tests for JSON serialization that verify the documented structure and will catch any breaking changes to the JSON API.

## Actions

- [ ] Create `tests/Application/SerializationContractTest.php`
- [ ] Test PathResult JSON structure matches documentation
- [ ] Test SearchOutcome JSON structure matches documentation
- [ ] Test SearchGuardReport JSON structure
- [ ] Test Money JSON structure matches documentation
- [ ] Test field types, not just that serialization works
- [ ] Test with various edge cases (zero amounts, large scales, etc.)

## Acceptance Criteria

- SerializationContractTest.php file created
- All documented JSON structures tested
- Tests verify structure, not just successful serialization
- Tests verify field names, types, and presence
- Edge cases covered
- All tests pass

## Definition of Done

- [ ] SerializationContractTest.php created
- [ ] All documented JSON structures tested
- [ ] Tests verify structure, not just serialization works
- [ ] All tests pass (vendor/bin/phpunit)
- [ ] Code follows project testing patterns
- [ ] Changes committed to version control

## Tests Needed

Create these test methods:
- `testPathResultJsonStructure()`
- `testSearchOutcomeJsonStructure()`
- `testSearchGuardReportJsonStructure()`
- `testMoneyJsonStructure()`
- `testJsonSerializationWithEdgeCases()`

